// imports

export default function controladorCadastro(params) {
  // receber os parametros: nome, cpf, celular,data de nascimento, email, senha;
  // sanitizar
  // validar
  // se tudo ok, chamar a rota create

  // const nome,cpf,celular,email,senha = {...params}; 
  console.log(params);

}